import React from 'react'
import { NoteComposition } from '../../../types/dto'

interface NoteProps {
  noteCompositionId: string;
  data: NoteComposition;
}

function NoteList({ data }: NoteProps) {
  return (
    <div>

    </div>
  )
}

export default NoteList